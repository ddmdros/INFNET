//quando usar o null
let usuario = null; // reserva um espaço na memoria e inicializa como vazio
let senha = null;

    //usuario = prompt("Digite o usuário");
    //senha = prompt("Digite a senha");

//quando usar o undefined
let idade; // quando alocamos espaço na memoria mas nao inicializamos, fica como undefined
console.log(idade);

//diferenca entre null e undefined
    //null é diferente de undefined:
console.log(null===undefined);